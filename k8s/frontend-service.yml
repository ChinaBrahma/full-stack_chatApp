apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: chat-app
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80          # Service port
      targetPort: 80    # Pod container port
  type: ClusterIP       # Internal cluster access
